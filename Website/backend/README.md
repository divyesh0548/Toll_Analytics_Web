"""
Backend layout (Flask + SQLAlchemy + Flask-Migrate)

Website/backend/
  run.py                 # Flask entry
  config.py              # Flask config (reads .env)
  alembic.ini
  scripts/create_db.py   # CREATE DATABASE from DB_NAME
  app/
    __init__.py          # create_app()
    extensions.py        # db, migrate
    models/user.py
    routes/auth.py
    services/            # analytics_db, charts
    utils/               # data_utils, analytics_config, db helpers
  migrations/
    alembic.ini          # Flask-Migrate config (must live here)
    versions/            # Alembic revisions
  legacy/streamlit_app.py

Create DB + migrate (do this yourself):

  cd Website/backend
  pip install -r requirements.txt
  python scripts/create_db.py
  flask --app run:app db upgrade

Later, after model changes:

  flask --app run:app db migrate -m "describe change"
  flask --app run:app db upgrade

Analytics tables (toll_analysis_main, gap_distribution_per_lane,
exempt_distribution_per_lane) are in migration 0002 — created by the same
`flask --app run:app db upgrade` command.

Companies + contacts (companies, company_contacts) are created by the
autogenerated revision after analytics tables — apply with `db upgrade`.
"""
